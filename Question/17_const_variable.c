#include <stdio.h>

int main() {
    const int num = 10; // constant declaration
    // num = 40;   Not allowed, will give error

    printf("The constant value of num is : %d\n", num);

    return 0;
}
