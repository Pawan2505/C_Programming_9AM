#include <stdio.h>

int main() {
    // While Loop: Print numbers from 10 to 1
    int num = 10;

    while (num >= 1) {
        printf("%d\n", num);
        --num; // decrement
    }

    return 0;
}
