#include <stdio.h>

int main() {

    int num; // Declaration of variable

    printf("Enter value of Num: ");
    scanf("%d", &num); // User input

    // printf("\nMemory address of num: %p\n", &num); // Optional: show memory location

    printf("The value of num is: %d\n", num);

    return 0;
}
