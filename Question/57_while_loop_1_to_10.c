#include <stdio.h>

int main() {
    // While Loop: Print numbers from 1 to 10
    int num = 1;

    while (num <= 10) {
        printf("%d\n", num);
        ++num; // increment
    }

    return 0;
}
