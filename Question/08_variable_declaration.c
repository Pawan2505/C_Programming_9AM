#include <stdio.h>

int main() {
    // Variable declaration
    int num;      // declare variable (empty container)
    
    // Initialization
    num = 10;     // assign value

    // %d -> format specifier for int
    printf("The value of num is : %d", num);

    return 0;
}
